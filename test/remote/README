This is the remote unit testing tool for libSBGN.js. To use this tool, you need to have maven installed, then simply run 'mvn test', and wait for several minutes, the cloud will do the work. The will be run from a remote repository which sync with the master branch on github every 10 mintues.
